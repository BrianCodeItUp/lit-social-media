import { prop, getModelForClass } from '@typegoose/typegoose'
import { ObjectType , Field } from 'type-graphql'

export class {{pascalCase name}} {
  @prop()
  hello: String;
}

export const {{pascalCase name}}Model = getModelForClass({{pascalCase name}})
