import { prop, getModelForClass } from '@typegoose/typegoose'
import { ObjectType as GqlType, Field as GqlField } from 'type-graphql'

export class {{pascalCase name}}Schema {
  @prop()
  username: String;
}

export const {{pascalCase name}}Model = getModelForClass({{pascalCase name}}Schema)
